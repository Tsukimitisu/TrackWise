import type { HTMLAttributes } from 'react'; interface SurfaceProps extends HTMLAttributes<HTMLDivElement> { elevated?: boolean; } export default function Surface({ className = '', elevated = true, ...props }: SurfaceProps) { return ( <div className={[ 'rounded-lg border border-gray-200 bg-white dark:border-gray-800 dark:bg-black', elevated ? 'shadow-subtle' : '', className, ] .filter(Boolean) .join(' ')} {...props} /> ); } 
