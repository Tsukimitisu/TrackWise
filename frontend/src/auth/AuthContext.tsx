import { createContext, useContext, useEffect, useMemo, useState } from 'react'; import type { ReactNode } from 'react'; import client from '../api/client'; import type { AuthUser } from '../types'; interface AuthContextValue { user: AuthUser | null; token: string | null; isLoading: boolean; setSession: (user: AuthUser, token: string) => void; clearSession: () => void; refreshSession: () => Promise<void>; } const AuthContext = createContext<AuthContextValue | null>(null); export function AuthProvider({ children }: { children: ReactNode }) { const [user, setUser] = useState<AuthUser | null>(null); const [token, setToken] = useState<string | null>(localStorage.getItem('trackwise_token')); const [isLoading, setIsLoading] = useState(true); const clearSession = () => { localStorage.removeItem('trackwise_token'); setUser(null); setToken(null); }; const setSession = (nextUser: AuthUser, nextToken: string) => { localStorage.setItem('trackwise_token', nextToken); setUser(nextUser); setToken(nextToken); }; const refreshSession = async () => { if (!localStorage.getItem('trackwise_token')) { setIsLoading(false); return; } try { const response = await client.get<AuthUser>('/auth/me'); setUser(response.data); } catch { clearSession(); } finally { setIsLoading(false); } }; useEffect(() => { void refreshSession(); }, []); const value = useMemo( () => ({ user, token, isLoading, setSession, clearSession, refreshSession }), [user, token, isLoading], ); return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>; } export function useAuth() { const context = useContext(AuthContext); if (!context) { throw new Error('useAuth must be used inside AuthProvider'); } return context; } 
