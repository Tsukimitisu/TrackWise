import { useEffect, useState } from 'react'; import { useParams, useNavigate } from 'react-router-dom'; import client from '../../api/client'; import { useAuth } from '../../auth/AuthContext'; interface DailyReport { id: number; user_program_id: number; report_date: string; tasks_done: string; tools_used?: string; problems_encountered?: string; learnings?: string; status: 'draft' | 'submitted' | 'approved' | 'rejected' | 'needs_revision'; submitted_at?: string; reviewed_by?: number; review_comment?: string; userProgram?: { user: { id: number; first_name: string; last_name: string }; program: { name: string }; }; files?: any[]; } const DailyReportDetailPage = () => { const { id } = useParams<{ id: string }>(); const [report, setReport] = useState<DailyReport | null>(null); const [loading, setLoading] = useState(true); const [reviewing, setReviewing] = useState(false); const [reviewComment, setReviewComment] = useState(''); const { user } = useAuth(); const navigate = useNavigate(); useEffect(() => { fetchReport(); }, [id]); const fetchReport = async () => { try { setLoading(true); const response = await client.get(`/daily-reports/${id}`); setReport(response.data); } catch (error) { console.error('Error fetching report:', error); navigate('/app/reports/daily'); } finally { setLoading(false); } }; const handleSubmit = async () => { try { const response = await client.post(`/daily-reports/${id}/submit`); setReport(response.data); } catch (error) { console.error('Error submitting report:', error); } }; const handleApprove = async () => { try { const response = await client.post(`/daily-reports/${id}/approve`, { review_comment: reviewComment, }); setReport(response.data); setReviewComment(''); setReviewing(false); } catch (error) { console.error('Error approving report:', error); } }; const handleReject = async () => { if (!reviewComment.trim()) { alert('Please provide a rejection reason'); return; } try { const response = await client.post(`/daily-reports/${id}/reject`, { review_comment: reviewComment, }); setReport(response.data); setReviewComment(''); setReviewing(false); } catch (error) { console.error('Error rejecting report:', error); } }; const handleRequestRevision = async () => { if (!reviewComment.trim()) { alert('Please provide feedback for revision'); return; } try { const response = await client.post(`/daily-reports/${id}/request-revision`, { review_comment: reviewComment, }); setReport(response.data); setReviewComment(''); setReviewing(false); } catch (error) { console.error('Error requesting revision:', error); } }; if (loading) return <div className="py-8 text-center text-slate-500 dark:text-slate-400">Loading...</div>; if (!report) return <div className="py-8 text-center text-black dark:text-black">Report not found</div>; const getStatusBadgeColor = (status: string) => { switch (status) { case 'approved': return 'bg-gray-200 text-black'; case 'rejected': return 'bg-gray-200 text-black'; case 'submitted': return 'bg-gray-200 text-black'; case 'needs_revision': return 'bg-gray-200 text-black'; default: return 'bg-gray-100 text-gray-800'; } }; return ( <div className="max-w-4xl mx-auto p-6"> <div className="flex justify-between items-center mb-6"> <h1 className="text-3xl font-bold text-gray-900">Daily Report - {report.report_date}</h1> <button onClick={() => navigate('/app/reports/daily')} className="text-gray-600 hover:text-gray-900" > ← Back </button> </div> <div className="bg-white rounded-lg shadow p-6 mb-6"> <div className="grid grid-cols-2 gap-4 mb-4"> <div> <p className="text-sm text-gray-500">Student</p> <p className="text-lg font-semibold"> {report.userProgram?.user.first_name} {report.userProgram?.user.last_name} </p> </div> <div> <p className="text-sm text-gray-500">Program</p> <p className="text-lg font-semibold">{report.userProgram?.program.name}</p> </div> <div> <p className="text-sm text-gray-500">Status</p> <span className={`px-3 py-1 rounded text-sm font-medium ${getStatusBadgeColor(report.status)}`}> {report.status} </span> </div> {report.submitted_at && ( <div> <p className="text-sm text-gray-500">Submitted At</p> <p className="text-lg font-semibold">{new Date(report.submitted_at).toLocaleString()}</p> </div> )} </div> <div className="border-t pt-4 mt-4"> <h3 className="text-xl font-semibold mb-4">Report Details</h3> <div className="mb-4"> <p className="text-sm text-gray-500 mb-1">Tasks Done</p> <p className="text-gray-900 whitespace-pre-wrap">{report.tasks_done}</p> </div> {report.tools_used && ( <div className="mb-4"> <p className="text-sm text-gray-500 mb-1">Tools Used</p> <p className="text-gray-900 whitespace-pre-wrap">{report.tools_used}</p> </div> )} {report.problems_encountered && ( <div className="mb-4"> <p className="text-sm text-gray-500 mb-1">Problems Encountered</p> <p className="text-gray-900 whitespace-pre-wrap">{report.problems_encountered}</p> </div> )} {report.learnings && ( <div className="mb-4"> <p className="text-sm text-gray-500 mb-1">Learnings</p> <p className="text-gray-900 whitespace-pre-wrap">{report.learnings}</p> </div> )} </div> {report.review_comment && ( <div className="border-t pt-4 mt-4"> <h3 className="text-lg font-semibold mb-2">Review Comment</h3> <p className="text-gray-900 whitespace-pre-wrap bg-gray-50 p-3 rounded">{report.review_comment}</p> </div> )} </div> {/* Action buttons */} <div className="flex gap-2 justify-center"> {report.status === 'draft' && ( <> <button onClick={() => navigate(`/app/reports/daily/${id}/edit`)} className="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700" > Edit </button> <button onClick={handleSubmit} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Submit Report </button> </> )} {report.status === 'submitted' && ['supervisor', 'coordinator', 'admin'].includes((user?.role as any)?.name || user?.role) && ( <> <button onClick={() => setReviewing(!reviewing)} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > {reviewing ? 'Cancel' : 'Review'} </button> </> )} {report.status === 'needs_revision' && ( <> <button onClick={() => navigate(`/app/reports/daily/${id}/edit`)} className="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700" > Edit for Revision </button> <button onClick={handleSubmit} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Resubmit </button> </> )} </div> {reviewing && report.status === 'submitted' && ['supervisor', 'coordinator', 'admin'].includes((user?.role as any)?.name || user?.role) && ( <div className="mt-6 bg-gray-200 rounded-lg p-6 border border-gray-300"> <h3 className="text-lg font-semibold mb-4">Review Report</h3> <textarea value={reviewComment} onChange={e => setReviewComment(e.target.value)} placeholder="Add your review comment (optional for approve, required for reject/revision)..." className="w-full px-4 py-2 border border-gray-300 rounded mb-4 focus:outline-none focus:ring-2 focus:ring-black" rows={4} /> <div className="flex gap-2"> <button onClick={handleApprove} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Approve </button> <button onClick={handleRequestRevision} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Request Revision </button> <button onClick={handleReject} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Reject </button> </div> </div> )} </div> ); }; export default DailyReportDetailPage; 
