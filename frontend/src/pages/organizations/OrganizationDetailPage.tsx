import { useEffect, useState } from 'react'; import { useParams, useNavigate } from 'react-router-dom'; import axios from 'axios'; interface Organization { id: number; name: string; type: string; address?: string; contact_phone?: string; contact_email?: string; status: string; created_at: string; updated_at: string; } const OrganizationDetailPage = () => { const { id } = useParams<{ id: string }>(); const [organization, setOrganization] = useState<Organization | null>(null); const [loading, setLoading] = useState(true); const navigate = useNavigate(); useEffect(() => { fetchOrganization(); }, [id]); const fetchOrganization = async () => { try { setLoading(true); const response = await axios.get(`${import.meta.env.VITE_API_BASE_URL}/organizations/${id}`); setOrganization(response.data); } catch (error) { console.error('Error fetching organization:', error); navigate('/app/organizations'); } finally { setLoading(false); } }; const handleDelete = async () => { if (!window.confirm('Are you sure you want to delete this organization? This action cannot be undone.')) return; try { await axios.delete(`${import.meta.env.VITE_API_BASE_URL}/organizations/${id}`); navigate('/app/organizations'); } catch (error) { console.error('Error deleting organization:', error); alert('Error deleting organization'); } }; if (loading) return <div className="text-center py-8">Loading...</div>; if (!organization) return <div className="text-center py-8 text-black">Organization not found</div>; return ( <div className="max-w-4xl mx-auto p-6"> <div className="flex justify-between items-center mb-6"> <h1 className="text-3xl font-bold text-gray-900">{organization.name}</h1> <div className="flex gap-2"> <button onClick={() => navigate(`/app/organizations/${id}/edit`)} className="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700" > Edit </button> <button onClick={handleDelete} className="px-4 py-2 bg-gray-200 text-white rounded hover:bg-gray-200" > Delete </button> <button onClick={() => navigate('/app/organizations')} className="px-4 py-2 bg-gray-300 text-gray-800 rounded hover:bg-gray-400" > Back </button> </div> </div> <div className="bg-white rounded-lg shadow p-6 mb-6"> <div className="grid grid-cols-2 gap-6"> <div> <p className="text-sm text-gray-500 mb-1">Type</p> <p className="text-lg font-semibold text-gray-900 capitalize">{organization.type || 'N/A'}</p> </div> <div> <p className="text-sm text-gray-500 mb-1">Status</p> <p className={`text-lg font-semibold ${organization.status === 'active' ? 'text-black' : 'text-black'}`}> {organization.status} </p> </div> <div> <p className="text-sm text-gray-500 mb-1">Email</p> <p className="text-lg font-semibold text-gray-900">{organization.contact_email || 'N/A'}</p> </div> <div> <p className="text-sm text-gray-500 mb-1">Phone</p> <p className="text-lg font-semibold text-gray-900">{organization.contact_phone || 'N/A'}</p> </div> <div className="col-span-2"> <p className="text-sm text-gray-500 mb-1">Address</p> <p className="text-lg font-semibold text-gray-900">{organization.address || 'N/A'}</p> </div> </div> </div> <div className="bg-gray-50 rounded-lg p-4 text-sm text-gray-600"> <p>Created: {new Date(organization.created_at).toLocaleDateString()}</p> <p>Last Updated: {new Date(organization.updated_at).toLocaleDateString()}</p> </div> </div> ); }; export default OrganizationDetailPage; 
