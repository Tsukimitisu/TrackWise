import { useEffect, useMemo, useState } from 'react'; import { useNavigate, useParams } from 'react-router-dom'; import client from '../../api/client'; interface AssignmentItem { id: number; user_id: number; required_hours: number; completed_hours: number; status: string; user?: { first_name: string; last_name: string; email?: string; phone?: string; }; program?: { name: string; }; supervisor?: { first_name: string; last_name: string; }; coordinator?: { first_name: string; last_name: string; }; } interface AttendanceLogItem { id: number; date: string; time_in?: string | null; time_out?: string | null; total_hours: number; status: string; approval_status: string; user_program_id?: number; } function normalizeList<T>(payload: unknown): T[] { if (Array.isArray(payload)) return payload as T[]; if (payload && typeof payload === 'object' && 'data' in payload && Array.isArray((payload as { data: unknown }).data)) { return (payload as { data: T[] }).data; } return []; } export default function AssignedTraineeDetailPage() { const { id } = useParams<{ id: string }>(); const navigate = useNavigate(); const [assignment, setAssignment] = useState<AssignmentItem | null>(null); const [logs, setLogs] = useState<AttendanceLogItem[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { void loadDetail(); }, [id]); const loadDetail = async () => { try { setLoading(true); if (!id) { navigate('/app/assigned-trainees'); return; } const [assignmentResponse, logResponse] = await Promise.all([ client.get(`/assignments/${id}`), client.get('/attendance-logs'), ]); const nextAssignment = assignmentResponse.data as AssignmentItem; setAssignment(nextAssignment); const assignmentLogs = normalizeList<AttendanceLogItem>(logResponse.data).filter( (log) => log.user_program_id === Number(id), ); setLogs(assignmentLogs); } catch (error) { console.error('Error loading assigned trainee detail:', error); setAssignment(null); setLogs([]); } finally { setLoading(false); } }; const metrics = useMemo(() => { const required = Number(assignment?.required_hours || 0); const completed = Number(assignment?.completed_hours || 0); const remaining = Math.max(0, required - completed); const progress = required > 0 ? Math.min(100, (completed / required) * 100) : 0; const approvedHours = logs .filter((log) => log.approval_status === 'approved') .reduce((sum, log) => sum + Number(log.total_hours || 0), 0); return { required, completed, remaining, progress, approvedHours }; }, [assignment?.completed_hours, assignment?.required_hours, logs]); if (loading) { return <div className="py-10 text-center text-slate-600">Loading assigned trainee...</div>; } if (!assignment) { return ( <div className="space-y-4 py-10 text-center"> <p className="text-slate-700">Assignment not found.</p> <button onClick={() => navigate('/app/assigned-trainees')} className="rounded-lg bg-ink-900 px-4 py-2 text-sm font-semibold text-white hover:bg-ink-700" > Back to Assigned Trainees </button> </div> ); } return ( <div className="space-y-6"> <section className="rounded-lg border border-white/70 bg-white/80 p-6 shadow-soft backdrop-blur-xl"> <div className="flex flex-wrap items-center justify-between gap-4"> <div> <p className="text-sm uppercase tracking-[0.2em] text-slate-500">Trainee Detail</p> <h1 className="mt-2 text-3xl font-black tracking-tight text-ink-900"> {assignment.user?.first_name} {assignment.user?.last_name} </h1> <p className="mt-2 text-slate-600">{assignment.program?.name ?? 'No program assigned'}</p> </div> <button onClick={() => navigate('/app/assigned-trainees')} className="rounded-lg border border-ink-200 bg-white px-4 py-2 text-sm font-semibold text-ink-800 hover:bg-ink-50" > Back </button> </div> </section> <section className="grid gap-4 md:grid-cols-4"> <MetricCard label="Required Hours" value={metrics.required.toFixed(2)} tone="ink" /> <MetricCard label="Completed Hours" value={metrics.completed.toFixed(2)} tone="green" /> <MetricCard label="Remaining Hours" value={metrics.remaining.toFixed(2)} tone="amber" /> <MetricCard label="Progress" value={`${metrics.progress.toFixed(1)}%`} tone="blue" /> </section> <section className="grid gap-4 md:grid-cols-3"> <InfoCard label="Email" value={assignment.user?.email ?? 'N/A'} /> <InfoCard label="Supervisor" value={assignment.supervisor ? `${assignment.supervisor.first_name} ${assignment.supervisor.last_name}` : 'N/A'} /> <InfoCard label="Coordinator" value={assignment.coordinator ? `${assignment.coordinator.first_name} ${assignment.coordinator.last_name}` : 'N/A'} /> </section> <section className="rounded-lg border border-white/70 bg-white/80 p-6 shadow-soft backdrop-blur-xl"> <div className="flex flex-wrap items-center justify-between gap-3"> <h2 className="text-lg font-semibold text-ink-900">Attendance Logs</h2> <div className="rounded-lg bg-slate-100 px-4 py-2 text-sm font-semibold text-slate-600"> Approved Hours: {metrics.approvedHours.toFixed(2)} </div> </div> <div className="mt-4 overflow-x-auto"> <table className="min-w-full divide-y divide-slate-200 text-sm"> <thead className="bg-slate-50/80 text-slate-500"> <tr> <th className="px-5 py-3 text-left font-medium">Date</th> <th className="px-5 py-3 text-left font-medium">Time In</th> <th className="px-5 py-3 text-left font-medium">Time Out</th> <th className="px-5 py-3 text-right font-medium">Hours</th> <th className="px-5 py-3 text-left font-medium">Status</th> <th className="px-5 py-3 text-left font-medium">Approval</th> </tr> </thead> <tbody className="divide-y divide-slate-100 bg-white"> {logs.map((log) => ( <tr key={log.id}> <td className="px-5 py-4">{log.date}</td> <td className="px-5 py-4">{log.time_in ?? 'N/A'}</td> <td className="px-5 py-4">{log.time_out ?? 'N/A'}</td> <td className="px-5 py-4 text-right">{Number(log.total_hours || 0).toFixed(2)}</td> <td className="px-5 py-4"> <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${statusTone(log.status)}`}> {log.status} </span> </td> <td className="px-5 py-4"> <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${approvalTone(log.approval_status)}`}> {log.approval_status} </span> </td> </tr> ))} {logs.length === 0 ? ( <tr> <td className="px-5 py-10 text-center text-slate-500" colSpan={6}> No attendance logs found for this trainee. </td> </tr> ) : null} </tbody> </table> </div> </section> </div> ); } function MetricCard({ label, value, tone, }: { label: string; value: string; tone: 'ink' | 'green' | 'amber' | 'blue'; }) { const toneClass = { ink: 'text-ink-900', green: 'text-black', amber: 'text-black', blue: 'text-black', }[tone]; return ( <div className="rounded-lg border border-white/70 bg-white/90 p-5 shadow-soft"> <p className="text-sm text-slate-500">{label}</p> <p className={`mt-2 text-2xl font-black ${toneClass}`}>{value}</p> </div> ); } function InfoCard({ label, value }: { label: string; value: string }) { return ( <div className="rounded-lg border border-white/70 bg-white/90 p-5 shadow-soft"> <p className="text-sm text-slate-500">{label}</p> <p className="mt-2 text-base font-semibold text-ink-900">{value}</p> </div> ); } function statusTone(status: string) { switch (status) { case 'active': return 'bg-gray-200 text-black'; case 'completed': return 'bg-gray-200 text-black'; case 'suspended': return 'bg-gray-200 text-black'; case 'dropped': return 'bg-gray-200 text-black'; default: return 'bg-slate-100 text-slate-700'; } } function approvalTone(status: string) { switch (status) { case 'approved': return 'bg-gray-200 text-black'; case 'pending': return 'bg-gray-200 text-black'; case 'rejected': return 'bg-gray-200 text-black'; default: return 'bg-slate-100 text-slate-700'; } }
