import { useEffect, useState } from 'react'; import client from '../../api/client'; import Badge from '../../components/ui/Badge'; import PageHeader from '../../components/ui/PageHeader'; import Surface from '../../components/ui/Surface'; import { FieldShell, TextField } from '../../components/ui/TextField'; const SystemSettingsPage = () => { const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [settings, setSettings] = useState<any>({}); useEffect(() => { fetchSettings(); }, []); const fetchSettings = async () => { try { setLoading(true); const res = await client.get(`/settings`); setSettings(res.data || {}); } catch (error) { console.error('Error fetching settings:', error); setSettings({}); } finally { setLoading(false); } }; const handleChange = (event: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => { const { name, value, type } = event.target as HTMLInputElement; setSettings((current: any) => ({ ...current, [name]: type === 'checkbox' ? (event.target as HTMLInputElement).checked : value })); }; const handleSave = async () => { try { setSaving(true); await client.put(`/settings`, settings); alert('Settings saved'); } catch (error) { console.error('Error saving settings:', error); alert('Error saving settings'); } finally { setSaving(false); } }; if (loading) return <div className="py-8 text-center text-slate-500 dark:text-slate-400">Loading...</div>; return ( <div className="space-y-6"> <PageHeader eyebrow="Administration" title="System Settings" description="Keep core platform settings grouped in a cleaner, more approachable admin screen." actions={<Badge tone="info">Admin only</Badge>} /> <Surface className="p-6"> <div className="grid gap-5 lg:grid-cols-2"> <FieldShell label="Site name"> <TextField name="site_name" value={settings.site_name || ''} onChange={handleChange} /> </FieldShell> <FieldShell label="Default required hours"> <TextField name="default_required_hours" type="number" value={settings.default_required_hours || ''} onChange={handleChange} /> </FieldShell> <label className="flex items-start gap-3 rounded-lg border border-slate-200 bg-slate-50 p-4 dark:border-slate-800 dark:bg-slate-900/60 lg:col-span-2"> <input id="allow_registration" name="allow_registration" type="checkbox" checked={!!settings.allow_registration} onChange={handleChange} className="mt-1 h-4 w-4 rounded border-slate-300 text-black focus:ring-black" /> <span> <span className="block text-sm font-semibold text-slate-900 dark:text-slate-50">Allow new user registration</span> <span className="mt-1 block text-sm text-slate-500 dark:text-slate-400">Enable or disable public account creation.</span> </span> </label> </div> <div className="mt-6 flex justify-end"> <button onClick={handleSave} disabled={saving} className="rounded-lg bg-slate-950 px-5 py-3 text-sm font-semibold text-white transition hover:-translate-y-0.5 hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-60 dark:bg-white dark:text-slate-900 dark:hover:bg-slate-200"> {saving ? 'Saving...' : 'Save Settings'} </button> </div> </Surface> </div> ); }; export default SystemSettingsPage; 
